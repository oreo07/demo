pipeline {
    agent any
    environment {
        APP_NANE = "thanabordee.w"
    }
    stages {
        stage('Build Image'){
            steps{
                sh "echo ${env.APP_NANE}"
            }
        }
    }
    
}
